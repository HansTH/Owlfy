{
  "name": "owlfy",
  "version": "1.0.0",
  "scripts": {
    "start": "lerna run start --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
